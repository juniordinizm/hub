name: Deploy Vercel production

on:
  workflow_dispatch:
    inputs:
      release_sha:
        description: Full 40-character SHA from main
        required: true
        type: string
      confirmation:
        description: Type DEPLOY_PRODUCTION_MAINTENANCE
        required: true
        type: string
      confirm_production:
        description: Confirm migration and deployment to Production
        required: true
        default: false
        type: boolean
      emergency_skip_backup:
        description: "Emergency exception: skip independent R2 backup"
        required: true
        default: false
        type: boolean
      emergency_skip_backup_confirmation:
        description: Type EMERGENCY_SKIP_PRODUCTION_BACKUP for the approved exception
        required: false
        type: string

concurrency:
  group: vercel-production
  cancel-in-progress: false

permissions:
  actions: read
  contents: read

jobs:
  deploy:
    name: Migrate and deploy approved main SHA
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    environment:
      name: vercel-production
    env:
      DEPLOYMENT_GIT_REF: main
      VERCEL_ORG_ID: ${{ vars.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ vars.VERCEL_PROJECT_ID }}
      VERCEL_SCOPE: neuro-capacitar
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          fetch-depth: 0
          ref: ${{ inputs.release_sha }}
      - name: Verify approved main SHA and green CI
        id: release
        env:
          CONFIRM_PRODUCTION: ${{ inputs.confirm_production }}
          CONFIRMATION: ${{ inputs.confirmation }}
          EMERGENCY_SKIP_BACKUP: ${{ inputs.emergency_skip_backup }}
          EMERGENCY_SKIP_BACKUP_CONFIRMATION: ${{ inputs.emergency_skip_backup_confirmation }}
          GH_TOKEN: ${{ github.token }}
          RELEASE_SHA: ${{ inputs.release_sha }}
        run: |
          set -euo pipefail

          if [[ "${CONFIRM_PRODUCTION}" != "true" ]] ||
             [[ "${CONFIRMATION}" != "DEPLOY_PRODUCTION_MAINTENANCE" ]]; then
            echo "Production deployment was not explicitly confirmed." >&2
            exit 1
          fi

          if [[ "${EMERGENCY_SKIP_BACKUP}" == "true" ]] &&
             [[ "${EMERGENCY_SKIP_BACKUP_CONFIRMATION}" != "EMERGENCY_SKIP_PRODUCTION_BACKUP" ]]; then
            echo "Emergency backup exception was not explicitly confirmed." >&2
            exit 1
          fi

          release_sha="${RELEASE_SHA}"
          if [[ ! "${release_sha}" =~ ^[0-9a-f]{40}$ ]]; then
            echo "release_sha must be a lowercase 40-character Git SHA." >&2
            exit 1
          fi

          git fetch --no-tags origin main
          if [[ "$(git rev-parse HEAD)" != "${release_sha}" ]] ||
             ! git merge-base --is-ancestor "${release_sha}" origin/main; then
            echo "release_sha must be a commit contained in origin/main." >&2
            exit 1
          fi

          ci_runs="$(
            curl --fail-with-body --silent --show-error \
              --header "Accept: application/vnd.github+json" \
              --header "Authorization: Bearer ${GH_TOKEN}" \
              --header "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/ci.yml/runs?branch=main&status=success&per_page=100"
          )"

          if ! jq -e --arg sha "${release_sha}" \
            '.workflow_runs | any(.head_sha == $sha and .conclusion == "success")' \
            <<< "${ci_runs}" > /dev/null; then
            echo "No successful CI run exists for the current main SHA." >&2
            exit 1
          fi

          echo "sha=${release_sha}" >> "$GITHUB_OUTPUT"
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.11
      - run: bun install --frozen-lockfile
      - name: Verify production deployment configuration
        env:
          DATABASE_URL_DIRECT: ${{ secrets.DATABASE_URL_DIRECT }}
          HEALTHCHECK_SECRET: ${{ secrets.HEALTHCHECK_SECRET }}
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
          RESTORE_R2_ACCESS_KEY_ID: ${{ secrets.RESTORE_R2_ACCESS_KEY_ID }}
          RESTORE_R2_SECRET_ACCESS_KEY: ${{ secrets.RESTORE_R2_SECRET_ACCESS_KEY }}
          BACKUP_R2_ACCOUNT_ID: ${{ vars.BACKUP_R2_ACCOUNT_ID }}
          BACKUP_R2_BUCKET_NAME: ${{ vars.BACKUP_R2_BUCKET_NAME }}
          PRODUCTION_NEON_BRANCH_ID: ${{ vars.PRODUCTION_NEON_BRANCH_ID }}
          PRODUCTION_NEON_PROJECT_ID: ${{ vars.PRODUCTION_NEON_PROJECT_ID }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          EMERGENCY_SKIP_BACKUP: ${{ inputs.emergency_skip_backup }}
        run: |
          set -euo pipefail
          required_names=(DATABASE_URL_DIRECT HEALTHCHECK_SECRET NEON_API_KEY PRODUCTION_NEON_BRANCH_ID PRODUCTION_NEON_PROJECT_ID VERCEL_AUTOMATION_BYPASS_SECRET VERCEL_ORG_ID VERCEL_PROJECT_ID VERCEL_SCOPE VERCEL_TOKEN)
          if [[ "${EMERGENCY_SKIP_BACKUP}" != "true" ]]; then
            required_names+=(BACKUP_R2_ACCOUNT_ID BACKUP_R2_BUCKET_NAME RESTORE_R2_ACCESS_KEY_ID RESTORE_R2_SECRET_ACCESS_KEY)
          fi
          for name in "${required_names[@]}"; do
            if [[ -z "${!name:-}" ]]; then
              echo "${name} is required by the vercel-production environment." >&2
              exit 1
            fi
          done
      - name: Require a recent independent Production backup
        if: inputs.emergency_skip_backup != true
        env:
          BACKUP_R2_ACCOUNT_ID: ${{ vars.BACKUP_R2_ACCOUNT_ID }}
          BACKUP_R2_BUCKET_NAME: ${{ vars.BACKUP_R2_BUCKET_NAME }}
          RESTORE_R2_ACCESS_KEY_ID: ${{ secrets.RESTORE_R2_ACCESS_KEY_ID }}
          RESTORE_R2_SECRET_ACCESS_KEY: ${{ secrets.RESTORE_R2_SECRET_ACCESS_KEY }}
        run: bun run ops:check:production-backup
      - name: Record emergency backup exception
        if: inputs.emergency_skip_backup == true
        run: |
          echo "::warning::Independent R2 backup gate skipped by explicit emergency authorization."
          {
            echo "### Emergency Production backup exception"
            echo "- independent R2 backup gate: SKIPPED"
            echo "- confirmation: EMERGENCY_SKIP_PRODUCTION_BACKUP"
            echo "- follow-up: run Backup Production database before the next release"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Set Production backup expiry
        run: echo "NEON_BACKUP_EXPIRES_AT=$(date -u --date '+14 days' +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_ENV"
      - name: Create confirmed Production Neon backup
        id: backup
        uses: neondatabase/create-branch-action@72ed4f69a12b6be9c16aebfad893f6a21e9aba8b # v6.4.0
        with:
          api_key: ${{ secrets.NEON_API_KEY }}
          branch_name: production-release-${{ github.run_id }}-${{ github.run_attempt }}
          expires_at: ${{ env.NEON_BACKUP_EXPIRES_AT }}
          parent_branch: ${{ vars.PRODUCTION_NEON_BRANCH_ID }}
          project_id: ${{ vars.PRODUCTION_NEON_PROJECT_ID }}
      - name: Verify Production Neon backup ancestry
        env:
          BACKUP_BRANCH_ID: ${{ steps.backup.outputs.branch_id }}
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
          PRODUCTION_NEON_BRANCH_ID: ${{ vars.PRODUCTION_NEON_BRANCH_ID }}
          PRODUCTION_NEON_PROJECT_ID: ${{ vars.PRODUCTION_NEON_PROJECT_ID }}
        run: |
          set -euo pipefail
          actual_parent="$(
            curl --fail --silent --show-error \
              --header "Authorization: Bearer ${NEON_API_KEY}" \
              --header "Accept: application/json" \
              "https://console.neon.tech/api/v2/projects/${PRODUCTION_NEON_PROJECT_ID}/branches/${BACKUP_BRANCH_ID}" \
              | jq -er '.branch.parent_id'
          )"
          [[ "${actual_parent}" == "${PRODUCTION_NEON_BRANCH_ID}" ]] || {
            echo "Production backup does not descend from the configured Production branch." >&2
            exit 1
          }
      - name: Apply production migrations
        env:
          DATABASE_URL_DIRECT: ${{ secrets.DATABASE_URL_DIRECT }}
        run: bun run db:migrate:production
      - name: Audit production migration state
        env:
          DATABASE_URL_DIRECT: ${{ secrets.DATABASE_URL_DIRECT }}
        run: bun run db:migrations:inspect -- --environment=vercel-production
      - name: Build unpromoted Production deployment
        id: deploy
        env:
          DEPLOYMENT_GIT_SHA: ${{ steps.release.outputs.sha }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          deployment_url="$(
            bunx vercel@57.0.0 --scope="$VERCEL_SCOPE" deploy --yes --prod --skip-domain \
              --meta "githubCommitRef=${DEPLOYMENT_GIT_REF}" \
              --meta "githubCommitSha=${DEPLOYMENT_GIT_SHA}" \
              --token="$VERCEL_TOKEN"
          )"
          echo "url=${deployment_url}" >> "$GITHUB_OUTPUT"
      - name: Smoke unpromoted Production readiness
        env:
          HEALTHCHECK_SECRET: ${{ secrets.HEALTHCHECK_SECRET }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
        run: |
          curl --fail-with-body --silent --show-error \
            --header "Authorization: Bearer ${HEALTHCHECK_SECRET}" \
            --header "x-vercel-protection-bypass: ${VERCEL_AUTOMATION_BYPASS_SECRET}" \
            "${{ steps.deploy.outputs.url }}/api/health/ready"
      - name: Verify unpromoted provider and migration state
        env:
          DATABASE_URL_DIRECT: ${{ secrets.DATABASE_URL_DIRECT }}
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
          PRODUCTION_NEON_BRANCH_ID: ${{ vars.PRODUCTION_NEON_BRANCH_ID }}
          PRODUCTION_NEON_PROJECT_ID: ${{ vars.PRODUCTION_NEON_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: >-
          bun run ops:check:production-release --
          --deployment=${{ steps.deploy.outputs.url }}
          --release-sha=${{ steps.release.outputs.sha }}
          --require-canonical-alias=false
      - name: Promote verified deployment
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: >-
          bunx vercel@57.0.0 --scope="$VERCEL_SCOPE" promote
          "${{ steps.deploy.outputs.url }}"
          --yes --token="$VERCEL_TOKEN"
      - name: Smoke Production maintenance profile
        env:
          HEALTHCHECK_SECRET: ${{ secrets.HEALTHCHECK_SECRET }}
          PRODUCTION_ORIGIN: https://app.neurocapacitar.com.br
        run: |
          set -euo pipefail
          for path in / /entrar /admin; do
            status="$(curl --silent --output /dev/null --write-out '%{http_code}' \
              "${PRODUCTION_ORIGIN}${path}")"
            [[ "${status}" == "503" ]]
          done
          curl --fail --silent --show-error \
            "${PRODUCTION_ORIGIN}/api/health" > /dev/null
          curl --fail --silent --show-error \
            --header "Authorization: Bearer ${HEALTHCHECK_SECRET}" \
            "${PRODUCTION_ORIGIN}/api/health/ready" > /dev/null
      - name: Verify promoted provider alias and migration state
        env:
          DATABASE_URL_DIRECT: ${{ secrets.DATABASE_URL_DIRECT }}
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
          PRODUCTION_NEON_BRANCH_ID: ${{ vars.PRODUCTION_NEON_BRANCH_ID }}
          PRODUCTION_NEON_PROJECT_ID: ${{ vars.PRODUCTION_NEON_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: >-
          bun run ops:check:production-release --
          --deployment=${{ steps.deploy.outputs.url }}
          --release-sha=${{ steps.release.outputs.sha }}
          --require-canonical-alias=true
